name: fifa_2026
description: "FIFA World Cup 2026 — Live scores, standings, teams & stadiums."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.9
  get: ^4.7.3
  http: ^1.6.0
  cached_network_image: ^3.4.1
  google_fonts: ^8.1.0
  intl: ^0.20.2
  shimmer: ^3.0.0
  get_storage: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
